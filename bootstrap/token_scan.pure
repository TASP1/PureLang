// Bootstrap seed for a PureLang-written lexer (self-hosting path)

fn is_alpha_len(n) {
    // placeholder classifier using length heuristics only
    return n
}

fn main() {
    src = "fn add(a, b) { return a + b }"
    n = str_len(src)
    print n
    mut i = 0
    while i < n {
        i = i + 1
    }
    print i
    // Map as simple symbol table seed
    m = map_new()
    map_set(m, "fn", 1)
    map_set(m, "return", 1)
    print map_len(m)
    print map_has(m, "fn")
}
