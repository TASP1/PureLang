// Modules: mod name { pub fn ... } called as name.fn(...)

mod math {
    pub fn add(a: Number, b: Number) {
        return a + b
    }

    pub fn double(x: Number) {
        return x * 2
    }
}

fn main() {
    print math.add(40, 2)
    print math.double(21)
}
