// Methods on structs: fn Type.method(self, ...)

struct Point {
    x
    y
}

fn Point.sum(self) {
    return self.x + self.y
}

fn Point.scale(self, factor) {
    return self.x * factor + self.y * factor
}

fn main() {
    p = Point(3, 4)
    print p.sum()
    print p.scale(2)
}
