// Channels + background thread

fn main() {
    ch = channel_new()
    print channel_len(ch)
    thread_spawn_send(ch, 40, 99)
    v = channel_recv(ch)
    print v
    print channel_len(ch)
    print ui_native_available()
}
